ul(class='posts #{ listClass }')
  each post, slug in public.blog._data
    if (slug !== 'index')
      li.post
        a.post-title(href='/blog/#{ slug }')= post.title
        |  
        span.post-date (#{ post.pubdate })